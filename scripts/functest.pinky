------------------------------------
--- Print the factorial of a number
------------------------------------
func factorial(n, x)
    mul := 1
    for i := 1, n do
        mul := mul * i
    end
    println("The factorial of " + n + " is " + mul)
end

factorial(5)